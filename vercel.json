{
  "buildCommand": "npm run build -w apps/web",
  "outputDirectory": "apps/web/.next",
  "framework": null,
  "installCommand": "npm ci --include-workspace-root && npm run db:generate -w packages/database",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet . ':(exclude)README.md'",
  "env": {
    "PRISMA_GENERATE_SKIP_AUTOINSTALL": "true"
  }
}